import { SizesTitulosDirective } from './sizes-titulos.directive';

describe('SizesTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new SizesTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
